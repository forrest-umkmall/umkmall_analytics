# Copy this file to .env and fill in your actual credentials
# cp .env.example .env

# Google Sheets Authentication
# Option 1: Use individual environment variables (recommended)
GOOGLE_CLIENT_EMAIL=your-service-account@project-id.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----"

# Option 2: Or use a credentials JSON file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# Optional: Additional Google Cloud metadata (not required for most use cases)
# GOOGLE_PROJECT_ID=your-project-id
# GOOGLE_PRIVATE_KEY_ID=your-private-key-id
# GOOGLE_CLIENT_ID=your-client-id

# Note: python-dotenv will automatically load these variables when you run main.py

# PostgreSQL Database Connections
# Supabase connection string (or any other Postgres database)
PRODUCT_DB_URL=postgresql://user:password@host.supabase.co:5432/postgres

# Analytics database (if you have a separate analytics DB)
ANALYTICS_DB_URL=postgresql://user:password@analytics-host:5432/analytics_db

# Note: Connection string format:
# postgresql://[user[:password]@][host][:port][/dbname][?param1=value1&...]
